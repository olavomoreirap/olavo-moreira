---
import Link from '@/components/Link.astro'
import PageHead from '@/components/PageHead.astro'
import ProjectCard from '@/components/ProjectCard.astro'
import { SITE } from '@/consts'
import Layout from '@/layouts/Layout.astro'
import { getAllCategories, getAllProjects, getRecentPosts } from '@/lib/data-utils'
import { buttonVariants } from '@/components/ui/button'
import { Icon } from 'astro-icon/components'

const [blog, projects, categories] = await Promise.all([
  getRecentPosts(SITE.featuredPostCount),
  getAllProjects(),
  getAllCategories(),
])
const featuredProjects = projects
  .filter((project) => project.data.tags?.includes('SaaS'))
  .slice(0, 2)
---

<Layout isWide>
  <PageHead slot="head" title="Home" />
  
  <div class="p-4 md:p-8 max-w-7xl mx-auto space-y-4">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
      
      <!-- HERO CARD (Top Left - 2 Cols) -->
      <div class="col-span-1 md:col-span-2 rounded-3xl border border-primary/20 bg-secondary/30 p-8 backdrop-blur-md relative overflow-hidden group">
        <div class="absolute -right-20 -top-20 h-64 w-64 rounded-full bg-primary/10 blur-3xl group-hover:bg-primary/20 transition-all duration-700"></div>
        <div class="relative z-10 flex flex-col justify-between h-full">
          <div>
            <h1 class="text-4xl md:text-5xl font-bold tracking-tight mb-4 text-foreground">
              Hey, I'm <span class="text-primary">Olavo</span>!
            </h1>
            <p class="font-medium mb-4 leading-relaxed">
              Backend Software Engineer | Fintech & Financial Services | Java • Spring Boot
            </p>
            <p class="text-base text-muted-foreground mb-4 leading-relaxed">
              Este blog nasceu da minha vontade de documentar meus estudos sobre Spring Boot e o ecossistema Java, compartilhando os estudos que eu fiz durante minha jornada como desenvolvedor até meu primeiro emprego na área.
            </p>
            <p class="text-base text-muted-foreground mb-4 leading-relaxed">
              Meu principal intuito é <strong>desmistificar a linguagem</strong>.
            </p>

            <p class="text-base text-muted-foreground mb-8 leading-relaxed">
              Sei que muitos desenvolvedores que estão começando sentem receio da verbosidade ou da complexidade do Java, por isso, foco em criar conteúdos que facilitem essa curva de aprendizado.
            </p>

          </div>
          
          <div class="flex gap-4 mt-auto">
             <Link href="/blog" class={buttonVariants({ variant: 'default' })}>
               Ler Blog
             </Link>
             <Link href="https://github.com/olavomoreirap" class={buttonVariants({ variant: 'outline' })}>
               GitHub
             </Link>
          </div>
        </div>
      </div>

      <!-- PROFILE CARD (Top Right - 1 Col) -->
      <div class="col-span-1 rounded-3xl border border-primary/20 bg-secondary/30 p-6 backdrop-blur-md flex flex-col items-center justify-center text-center relative overflow-hidden group">
         <div class="absolute inset-0 bg-gradient-to-br from-transparent to-primary/5 opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
         
         <div class="relative h-32 w-32 mb-4 rounded-full border-4 border-primary/20 p-1 group-hover:border-primary/50 transition-colors duration-300">
            <img 
              src="https://github.com/olavomoreirap.png" 
              alt="Olavo Moreira" 
              class="rounded-full w-full h-full object-cover bg-background"
              loading="lazy"
            />
         </div>
         
         <h2 class="text-xl font-bold">Olavo Moreira</h2>
         <p class="text-sm text-primary/80 font-mono mb-4">@olavomoreirap</p>
         
         <div class="flex gap-3 text-2xl">
            <!-- Social Icons placeholder - could be components -->
            <Link href="https://github.com/olavomoreirap" class="text-muted-foreground hover:text-primary transition-colors">
              <span class="sr-only">GitHub</span>
              <svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-github"><path d="M15 22v-4a4.8 4.8 0 0 0-1-3.5c3 0 6-2 6-5.5.5-1 .5-2 .5-2 0-2-1.5-3.5-1.5-3.5-4 .5-6 3.5-6 3.5s-2-3.5-6-3.5-1.5 3.5-1.5 3.5c0 0 0 1 .5 2-1.5 1-1.5 2.5-1.5 2.5 0 3.5 3 5.5 6 5.5-.5 1-.5 2.5-.5 2.5V22"/></svg>
            </Link>
            <Link href="https://linkedin.com/in/olavomoreira" class="text-muted-foreground hover:text-primary transition-colors">
              <span class="sr-only">LinkedIn</span>
              <svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-linkedin"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect width="4" height="12" x="2" y="9"/><circle cx="4" cy="4" r="2"/></svg>
            </Link>
         </div>
      </div>

      <!-- TECH STACK (Middle Left - 1 Col) -->
      <div class="col-span-1 rounded-3xl border border-primary/20 bg-secondary/30 p-6 backdrop-blur-md flex flex-col">
        <h3 class="font-bold text-lg mb-6 flex items-center gap-2">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-primary"><path d="m18 16 4-4-4-4"/><path d="m6 8-4 4 4 4"/><path d="m14.5 4-5 16"/></svg>
          Tech Stack
        </h3>
        
        <div class="space-y-5 flex-1">
           <div class="group">
             <div class="flex justify-between text-xs mb-2 font-mono"><span class="group-hover:text-primary transition-colors">Java / Spring Boot</span> <span class="text-muted-foreground">95%</span></div>
             <div class="h-2 w-full bg-background/50 rounded-full overflow-hidden border border-white/5">
                <div class="h-full bg-gradient-to-r from-purple-600 to-pink-500 w-[95%] shadow-[0_0_10px_rgba(236,72,153,0.5)]"></div>
             </div>
           </div>
           
           <div class="group">
             <div class="flex justify-between text-xs mb-2 font-mono"><span class="group-hover:text-primary transition-colors">Microservices</span> <span class="text-muted-foreground">85%</span></div>
             <div class="h-2 w-full bg-background/50 rounded-full overflow-hidden border border-white/5">
                <div class="h-full bg-gradient-to-r from-purple-600 to-pink-500 w-[85%]"></div>
             </div>
           </div>

           <div class="group">
             <div class="flex justify-between text-xs mb-2 font-mono"><span class="group-hover:text-primary transition-colors">Docker / K8s</span> <span class="text-muted-foreground">70%</span></div>
             <div class="h-2 w-full bg-background/50 rounded-full overflow-hidden border border-white/5">
                <div class="h-full bg-gradient-to-r from-purple-600 to-pink-500 w-[70%]"></div>
             </div>
           </div>
           
           <div class="group">
             <div class="flex justify-between text-xs mb-2 font-mono"><span class="group-hover:text-primary transition-colors">SQL / NoSQL</span> <span class="text-muted-foreground">80%</span></div>
             <div class="h-2 w-full bg-background/50 rounded-full overflow-hidden border border-white/5">
                <div class="h-full bg-gradient-to-r from-purple-600 to-pink-500 w-[80%]"></div>
             </div>
           </div>
        </div>
      </div>

      <!-- LATEST POSTS (Middle Right - 2 Cols) -->
      <div class="col-span-1 md:col-span-2 rounded-3xl border border-primary/20 bg-secondary/30 p-6 backdrop-blur-md relative overflow-hidden flex flex-col">
        <div class="flex items-center justify-between mb-6">
          <h3 class="font-bold text-lg flex items-center gap-2">
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-primary"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6"/><path d="M16 13H8"/><path d="M16 17H8"/><path d="M10 9H8"/></svg>
            Latest Logs
          </h3>
          <Link href="/blog" class="text-xs text-primary hover:underline underline-offset-4">View all &rarr;</Link>
        </div>
        
        <div class="grid gap-3 flex-1">
          {blog.slice(0, 3).map((post) => (
            <Link href={`/blog/${post.id}`} class="block group">
              <div class="p-3 rounded-xl bg-background/40 hover:bg-primary/10 border border-transparent hover:border-primary/20 transition-all duration-300 flex justify-between items-center">
                 <div>
                   <h4 class="font-medium text-sm group-hover:text-primary transition-colors line-clamp-1">{post.data.title}</h4>
                   <p class="text-xs text-muted-foreground mt-1">{post.data.date.toLocaleDateString()}</p>
                 </div>
                 <div class="opacity-0 group-hover:opacity-100 transition-opacity -translate-x-2 group-hover:translate-x-0">
                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-primary"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
                 </div>
              </div>
            </Link>
          ))}
        </div>
      </div>

      {
        featuredProjects.length > 0 && (
          <div class="col-span-1 md:col-span-3 rounded-3xl border border-primary/20 bg-secondary/30 p-6 backdrop-blur-md relative overflow-hidden">
            <div class="flex items-center justify-between mb-6">
              <h3 class="font-bold text-lg flex items-center gap-2">
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-primary"><rect width="18" height="14" x="3" y="5" rx="2"/><path d="M8 21h8"/><path d="M12 17v4"/></svg>
                Projetos em destaque
              </h3>
              <Link href="/about" class="text-xs text-primary hover:underline underline-offset-4">Ver todos &rarr;</Link>
            </div>
            <div class="grid gap-4 md:grid-cols-2">
              {featuredProjects.map((project) => (
                <ProjectCard project={project} />
              ))}
            </div>
          </div>
        )
      }

      {
        categories.length > 0 && (
          <div class="col-span-1 md:col-span-3 rounded-3xl border border-primary/20 bg-secondary/30 p-6 backdrop-blur-md relative overflow-hidden">
            <div class="flex items-center justify-between mb-6">
              <h3 class="font-bold text-lg flex items-center gap-2">
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-primary"><rect width="18" height="12" x="3" y="6" rx="2"/><path d="M12 11v6"/><path d="M8 19h8"/></svg>
                Trilhas de estudo
              </h3>
              <Link href="/trilhas" class="text-xs text-primary hover:underline underline-offset-4">Ver todas &rarr;</Link>
            </div>
            <div class="grid gap-4 md:grid-cols-2">
              {categories.map((category) => (
                <Link
                  href={`/trilhas/${category.id}`}
                  class="group relative rounded-2xl border border-primary/15 bg-background/40 p-5 transition-all duration-300 hover:border-primary/40 hover:bg-background/60 overflow-hidden"
                >
                  <div
                    class={`pointer-events-none absolute inset-0 bg-gradient-to-br ${category.accent} opacity-0 transition-opacity duration-500 group-hover:opacity-100`}
                  ></div>
                  <div class="relative flex h-full flex-col gap-3">
                    <div class="flex items-center gap-2 text-xs uppercase tracking-wide text-muted-foreground">
                      <Icon name={category.icon} class="size-4 text-primary" />
                      Trilha
                    </div>
                    <div>
                      <h4 class="text-lg font-medium group-hover:text-primary transition-colors">
                        {category.label}
                      </h4>
                      <p class="text-xs text-muted-foreground mt-2">
                        {category.description}
                      </p>
                    </div>
                    <div class="mt-auto flex items-center justify-between text-xs text-muted-foreground">
                      <span>{category.count} estudos</span>
                      <span class="text-primary">Abrir trilha →</span>
                    </div>
                  </div>
                </Link>
              ))}
            </div>
          </div>
        )
      }

    </div>
  </div>
</Layout>

